#include <iostream>
#include <vector>
using namespace std;
class Sum
{
    public:
    long long iterativeSum(const vector<int>& arr);
    long long recursiveSum(const vector<int>& arr, int n);
};